name: "TestsNDTensors"
on:
  push:
    branches:
      - "main"
    tags: "*"
  pull_request:
    types:
      - "opened"
      - "synchronize"
      - "reopened"
      - "ready_for_review"
      - "converted_to_draft"
  workflow_dispatch: ~
concurrency:
  group: "${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: "${{ startsWith(github.ref, 'refs/pull/') }}"
permissions:
  contents: "read"
  pull-requests: "read"
jobs:
  classify:
    name: "Classify"
    runs-on: "ubuntu-latest"
    outputs:
      substantive: "${{ steps.classify.outputs.substantive }}"
    steps:
      - uses: "ITensor/ITensorActions/.github/actions/classify-pr@mf/subdir-inputs"
        id: "classify"
        with:
          subdir: "NDTensors"
  tests:
    name: "TestsNDTensors"
    needs: "classify"
    if: "${{ needs.classify.outputs.substantive == 'true' }}"
    strategy:
      fail-fast: false
      matrix:
        version:
          - "lts"
          - "1"
    uses: "ITensor/ITensorActions/.github/workflows/Tests.yml@mf/subdir-inputs"
    with:
      subdir: "NDTensors"
      julia-version: "${{ matrix.version }}"
      os: "ubuntu-latest"
      nthreads: 2
      localregistry: "https://github.com/ITensor/ITensorRegistry.git"
    secrets:
      CODECOV_TOKEN: "${{ secrets.CODECOV_TOKEN }}"
  tests-gate:
    name: "TestsNDTensors"
    needs:
      - "classify"
      - "tests"
    if: "${{ always() }}"
    runs-on: "ubuntu-latest"
    steps:
      - name: "Verify result"
        run: |
          echo "classify.substantive = ${{ needs.classify.outputs.substantive }}"
          echo "tests.result = ${{ needs.tests.result }}"
          if [ "${{ needs.classify.outputs.substantive }}" != "true" ]; then
            echo "Non-substantive PR for NDTensors's scope; TestsNDTensors skipped (not run). Reporting success."
            exit 0
          fi
          test "${{ needs.tests.result }}" = "success"
